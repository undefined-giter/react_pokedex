	### Open the project ###
// unzip the folder
// npm install				// add all modules
// npm install -g json-server
add pokemoning folder (in src)
// npm start				// run in a terminal		
// npx json-server --watch db.json --port 3001 	// run in another terminal

(npm i bootstrap@5.2.0-beta1)
(npm install react-dom)
